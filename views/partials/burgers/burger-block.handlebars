<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="devour-burger" data-id="{{this.id}}">DEVOURED!</button>
</li>
